import { LogbookPage } from "./components/logbookPage";

export const metadata = {
  title: 'Logbook | I.M.S.',
}
const Logbook = () => {
  return(
      <main>
         <LogbookPage/>
      </main>
  )
}

export default Logbook;